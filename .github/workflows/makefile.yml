name: Makefile CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # - name: configure
    #   run: ./configure

    - name: Log my secret
      run: echo ${{ secrets.my_secret }}

    - name: Run make
      run: cd makefiledemo; make

    - name: Run check
      run: cd makefiledemo; make check

    # - name: Run distcheck
    #   run: make distcheck
